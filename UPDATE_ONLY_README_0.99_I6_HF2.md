# GameIndex Beta 0.99 I6 HF2 — UPDATE_ONLY

Baseline required: verified `GameIndex Beta 0.99 I6 HF1 FULL`.

Package: `GameIndex_Beta_0.99_I6_HF2_LAUNCH_VISUAL_REBRAND_UPDATE_ONLY.zip`

Overlay the package contents at the application root, preserving paths. No file deletions are required and no database migration is introduced by HF2.

The release build process verifies:

`HF1 FULL + HF2 UPDATE_ONLY = HF2 FULL`

with zero missing, zero extra and zero different files.
