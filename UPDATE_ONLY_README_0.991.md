# GameIndex Beta 0.991 — UPDATE_ONLY

Apply only over the verified GameIndex Beta 0.99 I6 HF2 FULL baseline. The release packaging process validates that HF2 + this overlay is byte-for-byte equivalent to the 0.991 FULL tree (excluding package container metadata).
