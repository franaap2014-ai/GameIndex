# UPDATE-ONLY — GameIndex Beta 0.9875 HF1.1

Use this package only when the deployed baseline is exactly Beta 0.9875 HF1.

It contains only added/changed files relative to the official HF1 FULL package. It does not contain a database, node_modules, runtime logs, secrets or temporary media.

Main changes: Image Manager contrast, URL preview/import UX, functional four-corner pointer resizing, release/cache markers and regression tests.

For the safest deployment, prefer the FULL package.
