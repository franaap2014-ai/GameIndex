# UPDATE_ONLY — GameIndex Beta 0.99 I6 HF1

Apply this package only over the verified `GameIndex Beta 0.99 I6 FULL` baseline.

The UPDATE_ONLY archive contains every file added or modified by HF1, including successor-release compatibility updates to historical regression tests and the HF1 release documentation.

No baseline file is intentionally deleted by this hot fix.

After overlaying UPDATE_ONLY on the I6 baseline, the resulting tree must be byte-equivalent to the HF1 FULL package. See `PACKAGE_VALIDATION_BETA_0.99_I6_HF1.md` for the final equivalence result.
