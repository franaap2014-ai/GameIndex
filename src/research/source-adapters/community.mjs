// Socket intentionally kept small in Beta 0.6.
// Community adapters must be game-specific or use an approved public API.
// The browser is never allowed to supply an arbitrary URL to fetch.
export async function researchCommunity() { return []; }
