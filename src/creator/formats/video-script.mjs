import { localize } from "./common.mjs";
export function createVideoScript(plan){
  const sections=plan.knowledge.map((k,i)=>({heading:`${localize(plan,{pt:"SEÇÃO",en:"SECTION",es:"SECCIÓN"})} ${i+1} — ${k.title}`,body:k.summary,knowledgeIds:[k.id]}));
  return {title:`${plan.topic} — ${localize(plan,{pt:"Roteiro de Vídeo",en:"Video Script",es:"Guion de Video"})}`,hook:localize(plan,{pt:`O que um jogador realmente precisa saber sobre ${plan.topic} em ${plan.game.nome}?`,en:`What does a player really need to know about ${plan.topic} in ${plan.game.nome}?`,es:`¿Qué necesita saber realmente un jugador sobre ${plan.topic} en ${plan.game.nome}?`}),introduction:localize(plan,{pt:"Neste vídeo, vamos passar apenas pelas informações específicas selecionadas pelo GameIndex para este tema.",en:"In this video, we will cover only the specific information GameIndex selected for this topic.",es:"En este video, cubriremos solo la información específica que GameIndex seleccionó para este tema."}),sections,conclusion:localize(plan,{pt:"Essa é a análise atual do GameIndex. Em temas que mudam com atualizações, confira as fontes e a data de verificação antes de publicar.",en:"That is the current GameIndex breakdown. For topics that change with updates, check sources and verification dates before publishing.",es:"Ese es el análisis actual de GameIndex. Para temas que cambian con actualizaciones, revisa las fuentes y las fechas de verificación antes de publicar."})};
}
