# Beta 0.9915 Profile Avatar Placeholders

These five SVG files are **class-level placeholders**, not the final 50 profile artworks.

The database catalog contains ten slots for each class (FREE, PRO, TESTER, DEV, CREATOR) so the product/API/eligibility flow can be implemented and tested before final artwork is supplied.

The `placeholder` field remains true for these entries. Do not describe them as finished profile photos in public release notes. Replace catalog asset URLs with final 1:1 artwork later while preserving avatar keys.
