const $=id=>document.getElementById(id),esc=v=>GV.safe(v??"");$("bugPageUrl").value=location.href;
function message(text,type=""){const el=$("bugMessage");el.textContent=text;el.className=`beta95-message ${type}`;}
$("bugReportForm").addEventListener("submit",async event=>{event.preventDefault();message("Enviando relatório sanitizado...");try{const data=await GV.api("/api/public/bug-reports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({category:$("bugCategory").value,title:$("bugTitle").value,description:$("bugDescription").value,steps:$("bugSteps").value,expectedResult:$("bugExpected").value,actualResult:$("bugActual").value,pageUrl:$("bugPageUrl").value,consent:$("bugConsent").checked,clientMetadata:{viewport:`${innerWidth}x${innerHeight}`,browserFamily:navigator.userAgent.includes("Chrome")?"Chromium":"Browser"}})});$("bugReceipt").innerHTML=`<div class="receipt-box"><strong>${esc(data.report.code)}</strong><p>Comprovante secreto:</p><code>${esc(data.report.receipt)}</code><p>Status: ${esc(data.report.status)}</p></div>`;$("receiptInput").value=data.report.receipt;event.target.reset();$("bugPageUrl").value=location.href;message(data.message,"good");}catch(e){message(e.message,"error");}});$("receiptForm").addEventListener("submit",async event=>{event.preventDefault();const el=$("receiptStatus");try{const data=await GV.api(`/api/public/bug-reports/${encodeURIComponent($("receiptInput").value.trim())}/status`);el.textContent=`${data.report.code}: ${data.report.status} · atualizado ${new Date(data.report.updatedAt).toLocaleString()}`;el.className="beta95-message good";}catch(e){el.textContent=e.message;el.className="beta95-message error";}});

