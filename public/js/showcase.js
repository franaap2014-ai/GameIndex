const esc=v=>GV.safe(v??"");GV.api("/api/showcase").then(data=>{document.getElementById("showcaseMetrics").innerHTML=Object.entries(data.metrics).map(([key,value])=>`<div class="b95-stat"><strong>${Number(value)}</strong><span>${esc(key)}</span></div>`).join("");document.getElementById("showcaseGames").innerHTML=data.games.map(game=>`<a class="beta95-card third showcase-game" href="/game.html?slug=${encodeURIComponent(game.slug)}"><p class="beta95-kicker">Game universe</p><h3>${esc(game.name)}</h3><p class="beta95-muted">${esc(game.description)}</p></a>`).join("");}).catch(error=>{document.getElementById("showcaseGames").innerHTML=`<p class="beta95-message error">${esc(error.message)}</p>`;});

