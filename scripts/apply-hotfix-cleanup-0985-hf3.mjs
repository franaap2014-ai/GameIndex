import {readFileSync,existsSync,rmSync} from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.dirname(path.dirname(fileURLToPath(import.meta.url)));const list=readFileSync(path.join(root,'DELETE_PATHS_0.985_HF3.txt'),'utf8').split(/\r?\n/).map(x=>x.trim()).filter(Boolean);for(const rel of list){const p=path.join(root,rel);if(existsSync(p))rmSync(p,{recursive:true,force:true});}console.log(`HF3 cleanup complete: ${list.length} legacy HF2 root paths checked.`);
